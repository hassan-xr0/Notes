So if we go back to [[7-8 .IPv4 Addressing]] where we learn about IP classes there we learn that IPv4 are divided into 5 classes but these classes waste multiple IP. For Example; If there is a point to point connection between to router so it only need to IPv4 Address So according to IPv4 classes the smallest netmask we can assign is 0/24. This makes a lot IPs waste

Its solution is CIDR (**Classless Inter-Domain Routing**). In this we can asking form largest subnet 0/1 (this means that only one bit in 32 bit address is fill other are free and can be assign ) to smallest 0/32(means 32 bit are fill with 1 cannot change IP only single value)
